# S12 nominal-voltage re-vote diagnostic

Question: does a first-enable request at the observed 1224mV setting behave
differently from the earlier 1352mV request? Equal decoded fields do not prove
an electrically unchanged or previously valid vote. Wi-Fi remains unqualified.

## Small implementation boundary

The existing readback kernel and initramfs are reused. The diagnostic DT adds
only S12 and an unbound `rog5,s12-revote-diagnostic` consumer to the native
baseline. It uses fixed1224000uV bounds, existing RET/AUTO policy and the same
fixed VPH parent. It adds no boot-on/always-on, GPIO, PHY, PCIe or radio activation.
All other baseline properties are unchanged. The real Wi-Fi PMU is absent.

The probe's query/mode/held-enable operations now require the exact cached
voltage and fresh successful S12 reads. Mode/enable changes are followed by
readback. Bit31 remains uninterpreted; unexpected other bits or field values
refuse the operation. A successful hold stays pinned even if post-readback
fails; module-init failure must not free that hold. No disable or RET restoration
path was added. No regulator-cache seeding or global kernel policy changed.

The trace verifier pairs responses with their originating active request.
Read message0x108 cannot satisfy a write0x10108 completion gate. It requires
the exact ordered before-read/write/after-read sequence and detects lost,
mismatched or duplicated transactions. Declared mode mirroring into sleep/wake
buffers remains visible; voltage/enable programming is forbidden in query/mode.

## Offline evidence

- The qualified module kit was restored in4.580s, without kernel compilation.
- Exact-provider callbacks prove setter/query caching makes no voltage/enable
  request while enabled-state is unknown; first enable sends1224 then1, and a
  voltage error prevents the enable request. The host test uses the kernel's
  existing sign-comparison warning policy; source callbacks were not edited.
- DT twins match:
  `7d30eee03d5a649c66a23cdaee87408a2a2693fd8103e1158fa83ad5996b65b5`.
- Module twins took25.140s/15.558s and match:
  `293b3d9659c8cd4fca592df6513fb4d7f537c8bb9fac208b3aef4efcb2bc44ca`.
- Exact-Image QEMU passed in2.069s, including ABI/BTF resolution and wrong-board
  refusal before any power/read operation. This is not physical qualification.
- Focused tests cover exact bounds, raw masks, fresh-state refusal, retained
  hold on post-read failure, precise DT delta and read/write ACK correlation.

## Admission and live scope

Generation is non-authoritative. One fresh claim may be published after the
frozen active-tier checkpoint; execution additionally requires exact-head CI,
artifact verification, exact device/boot/topology, safe power, all117 nodes RO,
fresh raw reads and an armed rollback. Before any write, re-read this boot's
state. Query, AUTO and held-enable are distinct, non-retryable operations.

Require distinct voltage/enable write completions, post-operation raw readback
and direct UFS reads. No PCIe/radio activation is permitted in this diagnostic.
Finish through V11 and explicitly restore the existing shared host profile;
replay `tests/fixtures/native-wifi/readback-host-recovery.json` first.
Original RPMh write-timeout behavior is unchanged; do not claim a hard CPU/bus
failure bound. Preserve all captured evidence and never retry ambiguous entry.
