# Exact OEM S12 voltage diagnostic

Question: can an exact1350mV request complete after the proved1224mV hold,
with stable UFS/NCM and no radio activation? This tests the request contract,
not physical voltage accuracy or the cause of the earlier1352mV reset.

## Implementation and boundaries

The qualified readback kernel and initramfs are unchanged. The same minimal
two-node diagnostic DT now permits1224–1360mV, matching the retained all60
stock-composition range. Its initial cached voltage remains1224mV; no boot-on,
always-on, GPIO, PCIe, radio or unrelated-node change is added.

`held-oem` performs the proved low-voltage enable/hold, reads fresh S12 state,
then submits exactly one fixed RPMh command: address0x40100, data1350. It cannot
round to1352 or select arbitrary resources/voltages. A new readback must return
the requested lower voltage field, enable1 and AUTO6. Other response bits remain
visible and uninterpreted. Failed reads/writes stop the sequence, and every
successful hold remains pinned even if a later check fails.

This raw diagnostic intentionally bypasses the regulator voltage cache.
`cache_not_updated=1` is explicit: no radio or further ordinary S12 consumer
operation is allowed; preserve the hold and reboot after capture. This is not
the production Wi-Fi voltage implementation. Original write-timeout behavior
and its limitations are unchanged.

## Offline evidence

- Focused tests exercise the exact fixed command, required hold/fresh reads,
  refusal and post-read failure paths, allowed DT range, and ordered request/
  completion sequences. Read responses cannot satisfy write acknowledgment.
- Module twins match:
  `0c6d91c195d35e774882d761fddaa2ce9df3117ad7458e66b2eb2241e326e639`.
  Build times16.328s and15.651s; no kernel/wrapper rebuild.
- DT twins match:
  `fd2fa33df884716d881a083505fbba582ebaba5292f6a939eb2b6a855505a0be`.
- Exact-Image QEMU passed in2.019s, resolving ABI/BTF and refusing the non-ASUS
  board before any control operation. The initial-provider callback proof
  from V12 remains applicable: its kernel and initial selector are unchanged.

The retained vendor source's setter uses `DIV_ROUND_UP(min_uv,1000)` and checks
the caller maximum. The authenticated CNSS request is1350000uV. This supports
testing that exact request; it does not prove the retained source is identical
to the installed stock binary or that a control vote is a physical measurement.

## Live admission remains separate

Require exact-head CI, signed artifact verification, current exact identity/
topology/boot, safe battery/temperature, all117 nodes read-only and an armed
rollback. Query, AUTO and held-OEM are non-retryable phases. Match the low hold
writes and the separate1350 write to their own responses and post-readbacks.
Keep slot A and persistent V11 unchanged. Restore the reviewed shared host
profile after fallback. No candidate is executed by building this diagnostic.

## Live result: passed, consumed

Executed once from `2bdd9a7b73fdb8141d9862ce96a91bd90e35d2bf`, with all checks
in run33384529419 successful. Active local tests passed in93.537s.
Target `eadec734-38d0-407e-9cbd-7e1aef1a3b29` reached SSH34.927s after entry.
The low hold completed, then the separate1350mV request completed and read
back `0x80000546`, with enable`0x80000001` and AUTO`0x80000006`.
Matched requests and responses are replayed from
`tests/fixtures/native-wifi/s12-oem-pass.json`.

Three direct1MiB p24 reads, NCM and power checks passed. Entry→completed
probe/read checks was44.657s. Source/target-before-vote PON snapshots matched.
Requested normal reboot restored V11`bdd1dd29-1def-4864-9d86-109ec9876137`
and shared SSH/state services in63.588s. No radio, GPT, slot, selector or
persistent deployment change occurred; every experimental operation used
all117-RO storage. Final battery was Full/Good,8.613V/30.1C, with only
sda+sda23 writable for normal service. Temporary capture/network setup ended.

This qualifies the fixed OEM request path after the low hold. It does not
identify the earlier reset cause, prove physical voltage accuracy, or qualify
radio operation with a stale regulator cache. The executed raw-write module
remains historical; the successor uses the regulator API.

## Cache-coherent kernel implementation, not yet admitted

Patch0036 adds only the observed1350mV request point for ASUS ROG Phone5,
PM8350 IDb, smps12/resource0x40100. Other devices/rails are unchanged. The
existing216 voltage values and320–2040mV envelope remain; the new table is
strictly increasing with217 entries. The exact1350 request maps without
substitution; wrong resource/base-table identity fails closed.

The patch changes descriptors only: it neither seeds enable/voltage caches
nor issues a power request at probe. The successor module requires exact
1350mV support, uses `regulator_set_voltage()` after the hold, and checks both
fresh readback and the resulting cached voltage. Raw writes are removed from
that successor. A cache/readback failure still retains the protective hold.

Exact added logic passes scope, ordering, preserved-setpoint and baseline-
mutation tests. The built-in driver object compiles in1.791s. New source
`1eea8970e87f1e1509fc12a85456f55570cfb4b1`, tree
`fd72ec7094faa8944727c8c8cc8b2730cc0a318b`, keeps config
`889d836fdc2928034d5d2a66062e4fa7d6ca204f82d506acc9fd17bb4a651bef`.
The cached development Image built in139.423s:
`2649a272eb2a6814db6302630a585fcab3d4422802e774ec55a55cc489f629e1`.

Independent clean-b and clean-c builds are running from that frozen source.
They must finish and match before candidate qualification; no new-kernel phone
boot or publication is implied by the development build. Existing archives,
module kit and source are preserved. New scratch is a bounded project-owned
12GiB tmpfs and must remain until verified archival.
