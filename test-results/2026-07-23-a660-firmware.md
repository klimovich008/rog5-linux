# A660 firmware gate - 2026-07-23

Result: **PASS** for offline provenance, path, size, hash, and container-format checks. No GPU-enabled image has been booted.

The candidate uses the three redistributable Adreno payloads from the pinned `linux-firmware` 20260622 release:

| Target path | Bytes | SHA-256 |
|---|---:|---|
| `qcom/a660_sqe.fw` | 43,292 | `d222f3fe290ef0516ee0ec43082596bad2df0fcbc2e0bbb26987623cef90cf76` |
| `qcom/a660_gmu.bin` | 55,252 | `8acab7b417d9ebde89a1de9ae1e2c261d352fcab122e31ecd580cec9fe2ae5e7` |
| `qcom/sm8350/a660_zap.mbn` | 1,054,648 | `5dbe91cb3fc9655ea2f2a9e1e169a0e30877bec84215899136a519444ca62a3d` |

The zap shader is a Qualcomm DSP6 ELF32 image and its installed path matches the GPU overlay exactly. The upstream SQE and GMU payloads differ from the older Android 11 ASUS copies extracted locally. Those stock files remain an untracked comparison/fallback only; they are not redistributed and are not the first mainline candidate.

The separate RAM-only GPU recovery initramfs is 5,899,836 bytes with SHA-256 `c520fd0e436887cea1131c87119974277e1247fbb99ac0e01be1a7ca878b69bb`. Two clean builds matched byte-for-byte, preserved the recovery `/init`, and contained exactly the three expected firmware files. It is deliberately not embedded in a boot image until the base recovery candidate passes on hardware.
