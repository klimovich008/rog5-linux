# Linear DMA-BUF preparation, 2026-09-12

Added `--dma-buf` to the actual Rust GLES probe: allocate/render an RGBA8 texture
through an FBO, finish producer work, create a preserved EGLImage, query/export
its layout and FD, import an explicit linear DMA-BUF into a second image/texture,
and check all 64 pixel channels through another FBO. Success follows all cleanup.
This is a same-context, one-plane sharing test; GBM allocation, cross-context
shared pixels with native fences, tiled modifiers and KMS remain unqualified.

Starting commit `970c1c8d530b3c23ad5ffc7c84f7ae17e1bd95ca`, tree
`31f276a7bd2ce80448a4c05e05e7a99ca3b16431`. Compiled source frozen at
`2abc23e4f409b74d4905ab62ae1158e6255794bb`, tree
`2505b0297b45096805e2c242aa0c446609e62f3a`. Later edits record qualification only.
The board, pinned Linux `7a5cef0db4795d9d453a12e0f61b5b7634fc4d40`, signed and
installed artifacts remain unchanged. This unsigned helper is not a candidate.

The two independent ARM64 builds are byte-identical: 4,661,432 bytes, SHA256
`35d3743896bf268f375311edea7381b4ef1ecc57a7bf6ace051cc67f8b1a4d9d`.
Cached Rust 1.98 builder image:
`0f429c6fd38e4400d8638bff1f7da0170375275ab29201e327a3e236ed9df16d`.

| Executed check | Result | Seconds |
|---|---|---:|
| New DMA-BUF test against old source | Expected FAIL: option absent, exit 2 | 1.859 |
| Initial fixture compile | FAIL: misleading-indentation under -Werror; fixed | 1.998 |
| First passing focused iteration, 11 groups | PASS | 7.261 |
| Focused behavior/mutation suite, 12 groups | PASS | 9.786 |
| Final texture-only mutation reporting check | PASS | 4.468 |
| ARM64 builds A / B | PASS; identical | 1.418 / 1.317 |
| Native / debug unit compilation | PASS | 1.167 / 0.666 |
| Debug Rust units / ARM64 ELF inspection | PASS | 0.004 / 0.016 |
| Native plain Mesa pixels | PASS | 0.164 |
| ARM64 fixture suite, eight groups | PASS | 7.697 |
| Actual ARM64 loader / Arch softpipe pixels | PASS | 0.032 / 0.967 |
| Actual ARM64 refusal of software in A660 mode | PASS | 0.666 |
| Actual native software DMA-BUF check | BLOCKED: export extension absent | 0.115 |
| Actual ARM64 software DMA-BUF check | BLOCKED: export extension absent | 0.666 |
| Frozen active host tier | 83 PASS, 0 FAIL, 0 BLOCKED, 0 whole-test SKIPPED, 256 NOT_SELECTED | 118.638 |

Three declared optional subchecks were SKIPPED, separate from whole-test counts.
The two actual capability BLOCKED results are separate from the integrated tier.
No full kernel rebuild or GitHub CI run is claimed this turn. Rustfmt/Clippy
remain NOT RUN: absent from the cached builder. No packages were downloaded or
installed; disk-backed scratch retained about 7 GiB free.

The before result establishes that a new capability was absent, not a defect in
the previous probe's narrower contract. Actual native and ARM64 software runs
fail specifically on missing `EGL_MESA_image_dma_buf_export`. They publish no
success output. The texture allocator could also choose a non-linear layout;
refusing such a layout would not prove absence of all DMA-BUF support.

The C ABI fixture transports shader-produced bytes through a real memfd and an
independently duplicated import reference. It tests the real Rust executable's
attributes, four-plane query buffer, one-plane/format/modifier refusal,
stride/offset/FD validation, partial-export FD cleanup, borrowed import ownership,
corrupt pixels and teardown errors. Removing the draw or image binding fails.
The memfd is not a GPU DMA-BUF. A separate source mutation verifies the new
texture/FBO rendering on real software Mesa while explicitly reporting DMA-BUF
NOT RUN. Native-fence checks remain separate; this mode uses `glFinish`.

Commands, source/tool identities, artifact hashes, timings and scope are in the
[qualification JSON](2026-09-12-dmabuf-qualification.json). Raw logs and JSON/JUnit
summaries are under `/home/deck/.local/state/rog5-dmabuf-evidence-20260912-r1` (`E`).
The focused command was `TMPDIR=E RUSTC=PREVIOUS_E/rustc python3 -O scripts/device/test-gles-readback.py`,
where `PREVIOUS_E` is `/home/deck/.local/state/rog5-fence-import-evidence-20260912-r1`.
The compiler wrappers have identical bytes. `python3 E/qualify.py` builds and
records commands; `python3 E/check-runtime.py` checks the 369 retained library
entries before target runtime execution. `python3 -O E/test-arm64.py`,
`python3 E/run-mesa.py` and `python3 E/check-capability.py` produce target/real
capability evidence. The private capability JSON retains the older generic
filename `native-capability.json`; every command inside selects `--dma-buf`.
`python3 E/run-active.py` runs the public active tier with two workers, 3 GiB
memory and no swap. `python3 E/final-checks.py` verifies metadata, historical
preservation, source freeze and artifact/runtime binding.

Changed source/tests: `tools/a660/rog5-gles-readback.rs`,
`tools/a660/test-fake-gles.c`, `scripts/device/test-gles-readback.py`,
`tools/a660/README-gles-readback.md`. Qualification files:
`configs/mobile/trial-plans.json`, `configs/project-status.json`,
`docs/current-state.md` (generated header only), `docs/development-lessons.md`,
`manifests/artifact-sets.json`, `manifests/current-artifact.json`, this report and
its qualification JSON. All 458 previous artifact sets are preserved; only the
GPU probe/runtime pointer fields advance. Old library identities are reused,
but execution is newly recorded for this executable.

The tested boundary comes from pinned Denial
`85b2303e2f09ae7b7b993641f90061a200f03d53`'s DMA-BUF external-texture path. API
references: [MESA export](https://registry.khronos.org/EGL/extensions/MESA/EGL_MESA_image_dma_buf_export.txt),
[EGL import](https://registry.khronos.org/EGL/extensions/EXT/EGL_EXT_image_dma_buf_import.txt),
[explicit modifiers](https://registry.khronos.org/EGL/extensions/EXT/EGL_EXT_image_dma_buf_import_modifiers.txt).
This probe's GLES allocation is independent of Denial's production allocator.

No phone contact, boot, power action, admission/claim/signing operation, new
candidate or protected-storage mutation occurred. All phone physical rows remain
NOT RUN; historical S06 and R01 remain FAIL. The full mobile package graph and
accepted headless/rescue contracts remain unchanged. Next independent GPU work
is controlled GBM allocation and shared pixels across contexts with native
fences. The next smallest physical experiment remains separately authorized
bounded corrected-display scanout/blank. No session is armed.
