# Subsystem port status

| Subsystem | 5.4.210 baseline | Linux 7.1 upstream base | ASUS work remaining |
|---|---|---|---|
| reversible boot | passing | v18 staging/rollback and v3 retained-exitrd normal reboot pass | repeat clean cycles and retain the same boundary for each new DTB tier |
| UFS root | passing vendor baseline; Phase-1 backup passed | no-mount mainline discovery passed historically; a fresh vendor-kernel read-only inventory now proves 7 UFS LUNs, 109 CRC-valid GPT entries, unstable `sd*` names, and complete private backups/restoration rehearsal for every protected small partition | enable stable normal mainline UFS, prove repeated reads, then boot the [bounded 16 GiB local Arch image](storage-migration-phase1.md) under standing authorization while retaining NFS and Alpine recovery |
| USB NCM/SSH | passing baseline; corrected target live-pending | Linux 7.1 normal-coldplug NCM/NFS and persistent client/server SSH identities pass twice; the [offline target gate](../test-results/2026-07-30-usb-ncm-ssh-offline.md) now binds the exact ConfigFS gadget/function/primary high-speed UDC, `usb0` state/MTU/address/route with no default, one current USB-peer SSH session, and matching Ed25519 key identities | observe the strengthened 88-field contract on the corrected temporary target, then convert the attended PC-backed transport into an independent deployment path |
| battery/charging | passing | guarded read-only SM8350 battery/USB/wireless telemetry passes through audited ADSP QRTR/PDR and battery-only PMIC GLINK; an opt-in dual-cell source/DT/sysfs candidate passes hostile offline gates and clean twin complete AArch64 builds with a byte-identical linked module | under a separately reviewed lifecycle, observe dual-cell topology and repeat physical-state/current-direction checks; then separately review charging; controls remain disabled |
| thermals/CPUfreq | passing read-only ancestry; control unproven | 33 thermal zones are readable and sane in two normal network-root boots; the [static thermal oracle](thermal-policy-static-oracle.md) now binds TSENS critical IRQ source/PDC routes, 12 exact CPU trip/cooling zones, five PMIC alarm zones, and the default shutdown call chain, while the offline CPU/runtime gates bind hardware CPUfreq Kconfig, exact eight-CPU/three-domain EPSS topology, and an 88-field live policy contract | observe exact sysfs policies, CPU cooling response, and PMIC registration on the corrected candidate; build the PMIC alarm driver in and profile the 10–30 second fallback without deliberately overheating the phone |
| OLED/DPU/DSI | vendor display baseline; mainline headless isolation passes offline | the [one-property headless DTB candidate](../test-results/2026-08-09-headless-display-isolation-offline.md) explicitly disables the otherwise implicit DISPCC provider while preserving disabled MDSS/DP/DSI/PHY state; hostile runtime fixtures reject any display platform device, DRM node, backlight, framebuffer, or display device node | run the isolation oracle on a temporary target, then separately port and test the AMS678 ER2 plus missing Pixelworks Iris/i6 bridge path; no mainline panel-off or power claim yet |
| touch/buttons/indicator | passing vendor baseline | PMK8350 `pmic_pwrkey` registration passed live; the [offline buttons/indicator candidate](buttons-indicator.md) binds accepted 7.1.4 source/config/modules to all three keys and a default-off PM8350C green LED; the [native runtime](headless-key-indicator.md) adds an exact-identity, press-only 180 ms service, and its sealed minimal successor root passes staged plus extracted verification | observe real press/release for all three keys and one bounded pulse under the corrected minimal-server gates, then test wake/power and FocalTech touch separately |
| GPU | pre-resource A660 GMU resume entry and complete rollback pass live; v9 is consumed | v3 accepts registration, v4 exact SQE/GMU requests, and v7 raw-size-pinned allocation/rollback with equal settled GEM state; v8 safely diagnosed signed-width and process-global PM-oracle errors; the [sole v9 cycle](../test-results/2026-07-27-a660-gmu-resume-entry-v9-live-accepted.md) accepts one GPU-device outer runtime-PM transition and complete rollback; the [v10 kernel tier](../test-results/2026-07-27-a660-gmu-cx-runtime-pm-v10-offline.md), [runtime oracle](../test-results/2026-07-27-a660-gmu-cx-runtime-pm-v10-runtime-offline.md), [protected-root/pre-live HOLD](../test-results/2026-07-27-a660-gmu-cx-runtime-pm-v10-prelive-hold.md), [current readiness HOLD](../test-results/2026-07-27-a660-gmu-cx-runtime-pm-v10-current-readiness-hold.md), and [attended-GO HOLD](../test-results/2026-07-27-a660-gmu-cx-runtime-pm-v10-prelive-go-hold.md) pass exact GMU/linked-CX classification, fourteen runtime mutations, recursive root verification, current one-shot controls, verifier-first server isolation, unarmed state preservation, and exact fallback preflight while config/Image/ABI and every other installed module remain accepted-v8-identical | v10 is selected and technically ready but deferred by the core-first roadmap; when resumed, use the central standing authorization for at most one admitted RAM-only cycle, require zero GX/clock/MMIO/IRQ/firmware-start/HFI activity, and consume v10 regardless of result |
| Wi-Fi/hotspot | passing | offline policy and inputs pass, hardware HOLD: the [vendor contract](../test-results/2026-07-27-arch-wifi-vendor-contract-hold.md) proves PCI `17cb:1103`/`17cb:0108`; the [isolated WCN6855/PCIe candidate](../test-results/2026-07-27-wcn6855-pcie-offline.md) passes mutation/schema and duplicate kernel-build checks; its [guarded runtime package](../test-results/2026-07-27-wcn6855-runtime-package-offline.md), [protected root](../test-results/2026-07-27-wcn6855-v1-prelive-hold.md), and [current-readiness HOLD](../test-results/2026-07-27-wcn6855-v1-current-readiness-hold.md) pass default radio lockout, enumeration-only oracle, independent watchdog, duplicate complete builds, recursive sealing, hostile mutations, verifier-first NFS, strict one-shot controls, byte-identical unarmed state, and exact fallback health; no radio runtime evidence | deferred by the core-first roadmap; when resumed, use the central standing authorization for one admitted RAM-only enumeration cycle with no scan/association/AP/retry/flash, consume it regardless of result, then design separate client, AP, provider-VPN, thermal, and battery gates |
| modem/DSPs | passing with delayed startup | ASUS reserved-memory contract and ADSP-only PAS/SCM startup pass | CDSP/modem/SLPI firmware names and one-processor-at-a-time validation |
| audio | basic services present | Qualcomm audio frameworks present | codecs, routing, speakers, microphones, headset safety |
| sensors/cameras | ASUS 5.4 VCNL36866 ALS/proximity board and protocol oracle pinned; other sensors not yet inventoried | accepted 7.1.4 is explicitly `port-required` for VCNL36866; a hostile-tested future read-only IIO source/DT/runtime contract exists, but no driver or hardware result | implement and compile the bounded VCNL36866 port first; then inventory IMU/compass from source/runtime evidence; fingerprint, NFC, and cameras remain separate later scopes |
| BTF/eBPF | BPF present, BTF absent | BTF generated and verified in 7.1 build | boot-time verifier/load tests, then optional GodShell systemd service |
| KDE remote UI | [Alpine 3.24 nested KWin/Plasma, noVNC, ttyd, CDP, screen-off operation, and reconnecting loopback SSH tunnel pass live](../test-results/2026-07-27-alpine-remote-gui-linux-tunnel-live.md) on installed 5.4.134 | userspace-independent; no Linux 7.1 DRM/KRDP acceptance yet | switch to physical DRM/KRDP and hardware rendering only after the display/GPU gates pass |

“Present upstream” means the SoC framework exists, not that the phone is supported. No row becomes passing until its device test succeeds without new kernel warnings, resets, or recovery loss.

The historical v2 staging root was writable physical UFS and its target DTB
enabled UFS and QMP/SuperSpeed. Its former zero-storage/USB2-only classifications
are withdrawn; v2 is superseded and must not be booted. Nothing was flashed.
