{
  "schema": 1,
  "phase": "Virtual DRM guest qualified; native Denial runtime lacks Xwayland",
  "artifact_pointer": "manifests/current-artifact.json",
  "mobile_contract": "configs/mobile/acceptance.json",
  "physical_tests": "NOT RUN",
  "phone_operations_authorized": false,
  "open_headless_results": {
    "S06": "FAIL",
    "R01": "FAIL"
  },
  "next": "The exact upstream ARM64 guest kernel builds and boots authenticated Arch payloads through read-only 9P. Modern Virtio enables virtual display/input discovery; the actual Denial CLI succeeds and the guest exits cleanly in 2.72 seconds. Actual full-shell startup reached Wayland socket creation but failed at unconditional Xwayland launch: the retained 315-package runtime omits it. The new prerequisite check reports BLOCKED before starting another VM. Next authenticate an explicit Xwayland/dependency extension and reuse this guest kernel for the full Denial startup question. No rendered shell frame, compositor input or Adreno proof is claimed. Frozen active tier: 87 PASS; three declared optional subchecks skipped. No phone operation is authorized; signed/installed/fallback bytes unchanged. S06/R01 remain FAIL; physical rows NOT RUN.",
  "evidence": "test-results/2026-09-12-virtual-drm.md"
}
