{
  "schema": 1,
  "phase": "GTK implicit-view cleanup fixed; full Wayland session pending",
  "artifact_pointer": "manifests/current-artifact.json",
  "mobile_contract": "configs/mobile/acceptance.json",
  "physical_tests": "NOT RUN",
  "phone_operations_authorized": false,
  "open_headless_results": {
    "S06": "FAIL",
    "R01": "FAIL"
  },
  "next": "The real GTK destruction regression fails before the guard and three lifecycle cases pass afterward. The actual settings app renders and exits without the invalid implicit-view removal message; its image is unchanged. The patched library and a byte-equivalent 34-file fixture are recorded separately from all previous artifacts. The frozen active tier passes 86 suites. Broadway EGL/ATK diagnostics and the unavailable Denial backend remain outside this fix. Upstream denial-nested is a topology demo, not the full shell. Next inspect retained ARM64 QEMU/kernel virtual DRM capabilities for an offline test of actual deniald. No phone operation is authorized; signed/installed/fallback bytes are unchanged. S06/R01 remain FAIL; physical rows NOT RUN.",
  "evidence": "test-results/2026-09-12-gtk-disposal.md"
}
