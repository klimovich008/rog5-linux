{
  "schema": 1,
  "phase": "Xwayland runtime authenticated; full virtual Denial framebuffer binding fails",
  "artifact_pointer": "manifests/current-artifact.json",
  "mobile_contract": "configs/mobile/acceptance.json",
  "physical_tests": "NOT RUN",
  "phone_operations_authorized": false,
  "open_headless_results": {
    "S06": "FAIL",
    "R01": "FAIL"
  },
  "next": "The 326-package runtime authenticates and starts Xwayland; all 55,666 previous filesystem entries are unchanged. The shell test now rejects zero-frame clean exits. VirGL advertises native fences but fails the initial framebuffer bind before Flutter startup; software llvmpipe lacks native fences. Targeted TRACE is compiled out in the retained release. Next prepare a bounded diagnostic renderer build after restoring disk headroom, reusing the kernel/runtime/cache. Frozen active tier: 87 PASS in 130.7 seconds; three declared optional subchecks skipped. No rendered full-session frame, compositor input or A660 proof is claimed. No phone operation is authorized; signed/installed/fallback bytes unchanged. S06/R01 remain FAIL; physical rows NOT RUN.",
  "evidence": "test-results/2026-09-12-xwayland-runtime.md"
}
