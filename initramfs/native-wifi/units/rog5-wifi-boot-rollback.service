[Unit]
Description=Return the RAM trial to the unchanged persistent V11 fallback
DefaultDependencies=no
Conflicts=shutdown.target
Before=shutdown.target

[Service]
Type=oneshot
ExecStart=/usr/bin/systemctl --no-block reboot
TimeoutStartSec=10s
